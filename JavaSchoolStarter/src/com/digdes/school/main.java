package com.digdes.school;

public class main {
    public static void main(String[] args) {
        JavaSchoolStarter starter = new JavaSchoolStarter();
        try {
//            starter.execute("INSERT VALUES 'lastName' = 'тимур' , 'id' = 13325,  'age' = 50");
//            starter.execute("INSERT VALUES 'lastName'= 'Фееедоров' , 'id'=1, 'age'=39, 'active'=true, 'cost' = 4.0");
//            starter.execute("INSERT VALUES 'lastName'= 'Фв' , 'id'=1, 'age'=40, 'active'=true, 'cost' = 4.0");
//            starter.execute("INSERT VALUES 'lastName'= 'Петор' , 'id'=3, 'age'=27, 'active'=true, 'cost' = 444.9");
//            starter.execute("SELECT");
//            starter.execute("SELECT WHERE 'age' >= 40 and 'lastName' like '%ФедО%' or 'id' = 1  and 'lastName' = 'Фв'");
//            starter.execute("SELECT WHERE 'age' >= 50 and 'lastName' = 'Фв' or 'cost' >= 4.0");
//            starter.execute("SELECT WHERE 'age' >= 40 or 'age' >= 40");
//            starter.execute("UPDATE VALUES 'active'=false, 'cost'=10.1 where 'id' = 3");
//            starter.execute("UPDATE VALUES 'lastName' = null where 'id' > 0 and 'id' < 100");
//            starter.execute("DELETE WHERE 'id'=3 or 'lastName' ilike 'ф'");
//            starter.execute("update values 'lasName' > 0");
//            System.out.println("********************AllData********************");
//            System.out.println(starter.execute(""));
        } catch (Exception ex) {
            ex.printStackTrace();
        }
    }
}

